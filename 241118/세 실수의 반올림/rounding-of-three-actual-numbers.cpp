#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    double a, b, c;
    cin >> a >> b >> c;
    cout << fixed;
    cout.precision(3);

    cout << a << '\n' << b << '\n' << c;
    return 0;
}